/** ISP corner radii — tokens/radii.css. Identity mode may reduce toward 0. */
export const radius = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
  xxl: 24,
  pill: 9999,
  identity: 0,
} as const;

export const borderWidth = {
  hairline: 1,
  focus: 2,
} as const;
